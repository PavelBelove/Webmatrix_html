# План рефакторинга WebMatrix

## Цель

Разделить монолитный файл WebMatrix_v_0.1.1.html на компоненты для упрощения дальнейшей разработки, сохранив при этом функциональность и внешний вид. Конечный результат должен собираться в единый HTML файл.

## Ключевые требования

1. **Браузерная совместимость**:

   - Основной фокус на браузеры на базе Chromium
   - Поддержка Firefox и Opera
   - Microsoft браузеры не приоритетны
   - Устаревшие браузеры не приоритетны

2. **Агенты**:

   - **Мастер промптов**:
     - Метапромпт может быть вшит в код
     - Возвращает подготовленный промпт для аналитика и список выходных колонок
   - **Аналитик**:
     - Использует промпт и настройки из секции настроек
     - Идет построчно по загруженной таблице, в шаблон промпта в {{}} подставляется значение из таблицы
     - Отвечает JSON с результатом анализа
     - Ответ парсится и вставляется в таблицу
   - **Чат**:
     - Имеет отдельные промпты
     - Промпты сохраняются в localStorage
     - Поддержка экспорта/импорта промптов
     - Возможность добавить в контекст пресет аналитика и(или) таблицу

3. **Хранение данных**:

   - Пресеты сохраняются в localStorage
   - Поддержка экспорта/импорта в JSON
   - Аналогично - кастомные промпты для чата
   - Принимает таблицы в Эксель и CSV
   - Экспортирует в Эксель и CSV

4. **API**:
   - Текущий провайдер: Gemini 1.5 PRO
   - Архитектура должна поддерживать легкое добавление новых провайдеров LLM

## Структура проекта

```
src/
├── styles/
│   ├── main.css
│   ├── table.css
│   └── components/
├── components/
│   ├── Spoiler.js
│   ├── Settings.js
│   ├── AnalysisTable.js
│   └── Chat.js
├── agents/
│   ├── PromptMaster.js
│   ├── Analyst.js
│   └── Chat.js
├── utils/
│   ├── api.js
│   └── storage.js
└── index.js
```

## План выполнения

1. **Этап 1: Стили**

   - Выделить все стили в отдельные CSS файлы
   - Сохранить текущий внешний вид

2. **Этап 2: Настройка проекта**

   - Инициализация npm проекта
   - Настройка webpack и babel
   - Настройка загрузчиков для CSS

3. **Этап 3: Компоненты**

   - Разделение на отдельные JS файлы
   - Перенос соответствующих стилей
   - Настройка взаимодействия между компонентами

4. **Этап 4: Агенты**

   - Выделение логики каждого агента
   - Создание единого интерфейса для LLM провайдеров
   - Перенос промптов

5. **Этап 5: Сборка**
   - Настройка сборки в единый HTML файл
   - Оптимизация и минификация
   - Тестирование функциональности

## Используемые технологии

- Webpack
- Babel
- CSS/Style loaders
- HTML Webpack Plugin
- Terser для минификации

## Важные моменты

1. После каждого этапа проект должен оставаться работоспособным
2. Сохранять совместимость с текущим API Gemini
3. Минимизировать изменения в логике работы
4. Обеспечить простоту добавления новых провайдеров LLM
5. Сохранять функциональность и внешний вид, минимум изменений при переносе.

## Общая логика использования программы:

WebMatrix - универсальный инструмент для анализа данных с помощью LLM, реализованный как single-page application. Основной принцип - максимальная гибкость через промпты при минимуме кода. Максимальная независимость и портативность.

- Пользователь пишет запрос в свободной форме, что нужно сделать с данными в поле промпта, заполняет желаемые колонки ответов по 1 в строке.
- Загружает изначальную таблицу в формате Эксель или CSV.
- Мастер промптов анализирует запрос и выдает промпт в формате, ожидаемом программой и список колонок, которые нужно будет заполнить.
- Аналитик идет построчно по загруженной таблице, в шаблон промпта в {{}} подставляется значение из таблицы
- Отвечает JSON с результатом анализа
- Ответ парсится и вставляется в таблицу.
- результат можно скачать в формате Эксель или CSV.
- Чат расширяет функционал - можно обсудить и улучшить пресет, или данные из таблицы.
- Пользователь может сохранять и переиспользовать пресеты аналитика
- Пользователь может использовать кастомные промпты для чата

## FIXME

- Стили из монолита не работают.
- Нет 1-й секции - спойлера с ридми.
- Нет секции промпта и колонок.
- Сейчас сделано, что пользователь может только серез алерт ввести запрос,но он должен писать его в полях промпта и колонок. Использование мастера промптов только опционально. Кроме того, мастер промптов может использоваться несколько раз для итеративного улучшения, например, пользователь уточняет прмпт, а мастер снова приводит его в соответствие с ожиданием программы.

## План переноса по секциям:

1. README и спойлеры:

   - Базовая структура и стили
   - Механизм сворачивания/разворачивания
   - Полный контент из монолита

2. Настройки и API:

   - Поля API ключа
   - Управление пресетами
   - localStorage интеграция

3. Промпты и колонки:

   - Поля ввода
   - Интеграция с мастером промптов
   - Сохранение состояния

4. Таблица и анализ:

   - Загрузка данных
   - Отображение результатов
   - Экспорт

5. Чат:
   - Интерфейс чата
   - Интеграция с контекстом
   - Работа с историей

## Критические компоненты для проверки:

- Правильность переноса метапромпта
- Сохранение всех функций из монолита
- Корректная работа localStorage
- Поддержка всех форматов файлов
- Правильная обработка ошибок

## Структура:

- Восстановить спойлер "📖 README"
- Добавить все подспойлеры:
  - 🎯 What can WebMatrix do?
  - 🔄 How does it work?
  - 📚 Ready-made recipes
  - 💬 AI assistant
  - 💡 Useful tips
  - 🚀 Quick start
  - ℹ️ Important to know

## Стили:

- Перенести стили для readme-container
- Восстановить стили для спойлеров
- Добавить стили для карточек и секций
- Проверить работу collapsible механизма

## Контент:

- Восстановить все секции из монолита
- Сохранить форматирование и эмодзи
- Проверить все списки и подсекции
